language: php

php:
  - '7.0'
  - '5.6'

env:
  matrix:
    - WP_THEME=https://github.com/WordPress/twentyseventeen/archive/master.zip

before_install:
  - nvm install node
  - nvm alias default node
  - node --version
  - npm install
  - npm run init
  - npm run wp-install
  - npm run wp >/dev/null 2>&1 &

script:
  - npm test
